import { updateStatus } from "@/actions/admin"
import { Button } from "@/components/ui/button"
export function StatusForm({ id, kind, current }: { id: string; kind: "order"|"repair"|"buyback"; current: string }) { const statuses = kind === "order" ? ["pending","confirmed","processing","ready","completed","cancelled"] : ["submitted","reviewing","quoted","in-progress","completed","rejected"]; return <form action={updateStatus} className="flex items-center gap-2"><input type="hidden" name="id" value={id} /><input type="hidden" name="kind" value={kind} /><select name="status" defaultValue={current} className="h-9 border border-input bg-background px-2 text-sm">{statuses.map((x)=><option key={x}>{x}</option>)}</select><Button size="sm" variant="outline">Update</Button></form> }
